window.onload = function () {

let table = document.getElementById("accounts");

for(let i=1;i<=100;i++){

acc++;

let account = {
number: acc,
name: "Customer " + i,
branch: "Main Branch",
balance: 1000 + (i*50),
status: "Active"
};

accountsDB.push(account);

let row=`
<tr id="row${account.number}">
<td>${account.number}</td>
<td>${account.name}</td>
<td>${account.branch}</td>
<td id="bal${account.number}">${account.balance}</td>
</tr>
`;

table.innerHTML += row;

}

};
